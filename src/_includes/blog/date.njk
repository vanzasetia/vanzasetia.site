<p class="time">
  Published on
  <time datetime="{{ page.date | htmlDateString }}"
    >{{ page.date | blogDate }}</time
  >. 
  {% if dateModified %}
  Updated on
  <time datetime="{{ dateModified | htmlDateString }}"
    >{{ dateModified | blogDate }}</time
  >.
  {% endif %}
</p>
